# 변경 로그

NetSense의 모든 주요 변경 사항을 여기에 기록합니다. 형식은
[Keep a Changelog](https://keepachangelog.com/)를 따르며, 이 프로젝트는
[의미론적 버전](https://semver.org/)을 준수합니다.

## [1.0.1] - 2026-09-26

### 변경

- **편집기 현재 네트워크 블록**이 트레이 팝업과 일치하는 전체 NIC 세부 정보 표시 (인터페이스 → SSID → 신호 → MAC → IPv4 → 넷마스크 → 게이트웨이 → IPv6 → DNS)
- **SSID 피커**를 datalist에서 실제 `<select>`로 변경, 숨겨진 네트워크 수동 입력 옵션 추가
- **인터페이스 피커**가 "en0 (Wi-Fi)"와 같은 인간 가독 라벨 표시
- **프린터 피커**를 datalist에서 실제 `<select>`로 변경, 인간 가독 라벨 표시, 자동 발견 큐 제외
- 사용되지 않는 i18n 키 `editor.nic_gateway_mac` 삭제; `editor.ssid_manual`, `editor.printer_placeholder`, `editor.printer_manual` 추가

## [1.0.0] - 2026-09-23

트레이에 상주하는 네트워크 프로파일 관리자(macOS / Windows / Linux): 네트워크는
인터페이스 이름이 아니라 정체 정보로 대조한다.

### 추가

- **프로필 모델.** 프로필은 `enabled` + `quick` + `detection` + `rules` + `THEN` + `ELSE`로
  구성됩니다. 규칙 사이는 OR이고, 한 규칙 안의 활성 조건은 모두 일치해야 합니다(AND). 조건 종류:
  `wifi_ssid`, `gateway_mac`, `bssid`, `network_interface`. 모든 조건, 규칙, 동작은 저마다
  `enabled` 플래그를 가지며 실시간 상태를 보고합니다.
- **엔진.** 적용되는 프로필은 정확히 하나입니다. 1개 일치는 그 프로필을 활성화하고, 2개 이상 일치는
  **충돌**(알리기만 하고 아무것도 적용하지 않음)이며, 0개 일치는 전역 `fallback`을 적용합니다.
  일치 판정 실패(충돌)와 실행 실패(오류)는 별개의 상태이고, 재판정은 식별 지문이 이끌며 게이트웨이와
  인터페이스 집합의 변화도 함께 감지합니다.
- **3A 네트워크 구성은 하나의 장벽으로.** IPv4 / 서브넷 마스크 / 게이트웨이 / DNS / IPv6와 정적
  라우트를 적용한 뒤 검증합니다. 시스템에서 다시 읽어 비교하고, 원한다면 ICMP/HTTP 헬스 체크를 곁들여
  지속 실패 시 DHCP로 복구합니다. 3A가 실패하면 3B로 넘어가지 않습니다.
- **3B1 일회성 동작**(`launch_app`, `run_script`, `set_default_printer`)은 `priority` 단위로 묶어 실행 — 값이 낮은 쪽 먼저,
  같은 우선순위는 병렬, 배치 사이는 순차이며 한 배치의 실패가 다음 배치를 막지 않습니다. 엔진 스레드
  밖에서 동작별로 제한 시간을 두고 실행되고, 매 실행이 동작 단위 구조 기록으로 남아 편집기와 패널이
  실시간 상태로 표시합니다. 스크립트 경로는 허용 스크립트 목록으로 제한하며, 상대 경로는 허용
  판단 전에 구성 파일이 있는 디렉터리를 기준으로 해석합니다. 권한 상승이 필요한 스크립트는 반드시
  시스템 승인 대화 상자를 거치고, 동작마다 승인을 한 번만 요구합니다. 기본 프린터 지정은 **이름**으로
  대상을 집습니다(편집기는 시스템의 프린터를 나열하고 현재 기본값을 표시합니다). 여기서도 바뀌는 것은
  *해당 사용자*의 기본값뿐 — macOS / Linux 는 `lpoptions`, Windows 는 사용자 단위 CIM 메서드 —
  따라서 네트워크를 전환해도 승인 창이 뜨지 않습니다.
- **3B2 상시 동작**(`periodic_script`, `keep_wireguard_connected`, `keep_vpn_connected`)이 지키는 것은
  원하는 상태이지, 같은 명령을 반복하는 것이 아닙니다. 사용 켜진 동작당 워커 하나씩을 Active 프로필의
  THEN 분기와 함께 시작하고, 새 구성을 적용하기 전에 반드시 멈춥니다. 각 점검은 `satisfied`, `repaired`,
  `faulted` 중 하나로 답하고 — 이미 올라 있는 터널은 명령을 하나도 내리지 않습니다 — 편집기와 패널이 그
  상태를 동작마다 보여줍니다. 워커는 승인 대화 상자를 절대 띄우지 않습니다. 더 높은 권한이 필요한 터널은
  주기마다 사용자를 두드리지 않고 오류로 보고합니다.
- **프로필별 감지 방식**: `network_events`, `polling_only` 또는 둘 다. 각각 변경 후 지연을
  둘 수 있어 순간적인 재연결이 어댑터를 계속 흔드는 일을 막습니다.
- **트레이 팝업 패널** — 실시간 상태, 모든 활성 인터페이스, VPN 터널, 일치 표시 배지가 있는 원클릭
  프로필 전환, 설정, 로그, DHCP/진단 동작, 업데이트 도구.
- **두 개의 설정 파일 — " 바꾸면 자동화 동작이 바뀌는가" 로 나눕니다**: `config.json`은 자동화 모델
  (프로필, 조건, 동작)을 담고 편집기가 저장합니다. `settings.json`은 앱 자체의 동작(인터페이스 언어,
  로그 보관 일수)을 담고 전용 설정 창에서 고릅니다. 로그인 시 시작은 어느 파일에도 저장하지 않습니다 —
  그 창을 열 때마다 운영 체제에 묻습니다. 파일에 사본을 남기는 것은 시스템과 다르게 말할 수 있는
  '두 번째 진실'을 직접 만들어 주는 일이기 때문입니다.
- **구성 편집기**는 모델 전체를 다루며 엔진 판정을 그 자리에 표시합니다. 충돌 대화 상자,
  3상태 DNS 제어, 그리고 수동 적용 전에 3A 대상과 실행할 배치들을 먼저 펼쳐 보이는 확인 창을
  제공합니다.
- **플랫폼 추상 계층**은 읽기, 쓰기, 인터페이스 열거, 권한 상승을 macOS(`networksetup` / `ipconfig` /
  `arp` / `system_profiler`), Windows(PowerShell CIM + `netsh`), Linux(`nmcli` / `ip`)에서
  구현합니다. 사용할 수 있으면 비밀번호 없는 경로를 쓰고, 아니면 시스템 승인 대화 상자를 폴백으로
  삼습니다.
- **온라인 업그레이드**: 릴리스 확인, 네이티브 설치와 재시작, 진행 상황을 패널로 스트리밍합니다 —
  에셋을 내려받아 SHA256을 검증하는 방법, 그리고 Homebrew 설치본의 경우 `brew upgrade --cask`를
  쓰는 방법이 있습니다.
- **5개 인터페이스 언어**(en / zh / zh-TW / ja / ko), 기본값은 영어이며 키 정합성과 자리 표시자
  검증을 하고, 설정 창에서 언어를 바꿉니다. 모든 창과 트레이 툴팁, 네이티브 오류 대화상자가 이 하나의
  사전에서 문구를 가져옵니다. 로컬 로그는 매일 회전하며 보관 일수도 같은 창에서 정합니다.
  GitHub Actions 매트릭스 빌드로 태그 하나를 올려 네 가지
  대상의 설치 파일을 모두 만듭니다.

### 알려진 미비점

- 터널 재연결(`scutil` 의 `--nc` 계통, `rasdial`, `wireguard.exe`, `nmcli`)은 단위 테스트와 각 플랫폼의
  CI 컴파일까지만 확인했습니다 — 플랫폼 계층의 나머지 부분과 마찬가지로 실기기에서 한 번씩 돌려 볼
  필요가 있습니다. macOS 에서는 일부 VPN 설정이 사용자가 직접 한 번 연결하기 전까지 상시 동작의
  연결 요청을 받아들이지 않을 수 있습니다.
- 구성 적용에는 DHCP로 전면 전환하는 폴백 외에는 되돌릴 수단이 없습니다 — 3A는 통과한 뒤에도
  헬스 체크가 계속 실패하는 프로필은, 이전에 동작하던 설정을 복구하는 대신 자동 획득으로
  되돌려집니다.
