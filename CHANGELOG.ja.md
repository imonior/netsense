# 変更履歴

NetSense のすべての重要な変更をここに記録する。形式は
[Keep a Changelog](https://keepachangelog.com/) に基づき、本プロジェクトは
[セマンティックバージョニング](https://semver.org/) に準拠する。

## [1.0.0] - 2026-09-23

トレイ常駐型のネットワークプロファイル マネージャー（macOS / Windows / Linux 対応）。
ネットワークはインターフェース名ではなく識別情報で照合する。

### 追加

- **プロファイル モデル。** プロファイルは `enabled` + `detection` + `rules` + `THEN` + `ELSE` で
  構成される。ルール間は OR。1 つのルール内では有効な条件がすべて一致する必要がある（AND）。
  条件の種類は `wifi_ssid`、`gateway_mac`、`bssid`、`network_interface`。条件・ルール・アクションは
  すべて独自の `enabled` フラグを持ち、ライブの状態を返す。
- **エンジン。** 適用されるプロファイルは常に 1 つだけ。1 件一致すればそれを有効化、2 件以上の一致は
  **競合**（報告のみで何も適用しない）、0 件ならグローバルな `fallback` を適用する。照合の失敗（競合）と
  実行の失敗（エラー）は別々の状態。再評価は識別フィンガープリントが駆動し、ゲートウェイと
  インターフェース集合の変化も検知する。
- **3A のネットワーク設定は 1 つのバリアとして。** IPv4 / サブネットマスク / ゲートウェイ / DNS / IPv6
  と静的ルートを適用し、続いて検証を行う。システムへ読み戻して比較し、必要であれば ICMP/HTTP の
  ヘルスチェックを併用して、継続して失敗すれば DHCP へ復帰させる。3A が失敗すると 3B は実行されない。
- **3B1 単発アクション**（`launch_app`、`run_script`）は `priority` 単位のバッチで実行 — 数値が低いものを
  先に、同じ優先度なら並行に、バッチ間は順次にし、あるバッチの失敗が次のバッチを止めることはない。
  エンジン スレッドの外でアクション単位のタイムアウト付きで動き、毎回の実行がアクション単位の
  構造化された記録を残すため、エディターとパネルはそれをライブの状態として表示する。スクリプトの
  パスは許可スクリプト（許可リスト）で制限し、相対パスは許可判定の前に設定ファイルのあるディレクトリ
  基準に解決する。特権を要するスクリプトは常にシステムの承認ダイアログを経由し、アクションごとに
  1 回の承認を求める。
- **3B2 常駐アクション**（`periodic_script`、`keep_wireguard_connected`、`keep_vpn_connected`）が保つのは
  望ましい状態で、同じコマンドを繰り返すことではない。有効なアクションごとにワーカー 1 本を動かし、
  Active プロファイルの THEN 分岐と同時に始め、新しい設定を適用する前に必ず止める。各チェックは
  `satisfied` / `repaired` / `faulted` のどれかで答え、すでに立っているトンネルはコマンドを 1 つも発行
  しない。エディターとパネルがその状態をアクションごとに表示する。ワーカーは承認ダイアログを
  決して出さない — より高い権限がいるトンネルは、間隔ごとにたたく代わりにエラーとして報告する。
- **プロファイルごとの検出方法**: `network_events`、`polling_only`、またはその両方。それぞれに変化後の
  待機時間を設定でき、一瞬の再接続がインターフェースを何度も揺らすのを防ぐ。
- **トレイのポップアップ パネル** — ライブの状態、すべての有効なインターフェース、VPN トンネル、一致を
  示すバッジ付きのワンクリック プロファイル切替、設定、ログ、DHCP/診断アクション、アップデータ。
- **設定エディター** — モデル全体を扱え、エンジンの判定をその場に表示する。競合ダイアログ、
  3 状態の DNS 控件、そして手動適用の前に 3A の対象と実行予定のバッチを一覧化する確認を備える。
- **プラットフォーム抽象層** — 読み取り、書き込み、インターフェース列挙、権限昇格を macOS
  （`networksetup` / `ipconfig` / `arp` / `system_profiler`）、Windows（PowerShell CIM + `netsh`）、
  Linux（`nmcli` / `ip`）で実装している。使える場合はパスワード不要の経路を採り、そうでなければ
  システムの承認ダイアログをフォールバックにする。
- **オンライン アップグレード**: リリース確認、ネイティブ インストールと再起動、パネルへの進捗
  ストリーミング —— アセットをダウンロードして SHA256 を検証する方法と、Homebrew 導入なら
  `brew upgrade --cask` を使う方法がある。
- **5 つの UI 言語**（en / zh / zh-TW / ja / ko）、既定は英語で、キーの対応とプレースホルダーの検証を
  行う。ローカルログは日替わりでローテート。GitHub Actions のマトリックスビルドで、1 つのタグから
  4 ターゲットすべてのインストーラーを生成する。

### 既知の未実装点

- トンネル再接続（`scutil` の `--nc` 系、`rasdial`、`wireguard.exe`、`nmcli`）はユニットテストと各
  プラットフォームの CI コンパイルまでしか確認していない — プラットフォーム層の他の部分と同じく、
  実際に機上で一度ずつ回す必要がある。macOS では一部の VPN 設定が手動で一度接続されるまで、
  常駐アクションによる接続要求を受け付けないことがある。
- 設定の適用には、DHCP への全面的な退避以外のロールバック手段がない — 3A は通ってもその後
  ヘルスチェックが継続して失敗するプロファイルは、動作中の設定を復元するのではなく、
  自動取得に戻される。
