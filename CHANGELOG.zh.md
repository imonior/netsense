# 更新日志

NetSense 的所有重要变更记录于此。格式基于
[Keep a Changelog](https://keepachangelog.com/)，本项目遵循
[语义化版本](https://semver.org/)。

## [未发布]

### 变更

- **编辑器当前网络区块**现在显示完整的网卡详情，与托盘面板一致（接口 → SSID → 信号 → MAC → IPv4 → 子网掩码 → 网关 → IPv6 → DNS）
- **SSID 选择器**从 datalist 改为真正的下拉框，支持手动输入隐藏网络
- **接口选择器**显示人类可读的标签（如 "en0 (Wi-Fi)"）而非裸设备名
- **打印机选择器**从 datalist 改为真正的下拉框，显示人类可读标签，排除自动发现的队列
- 删除未使用的 i18n key `editor.nic_gateway_mac`；新增 `editor.ssid_manual`、`editor.printer_placeholder`、`editor.printer_manual`

## [1.0.0] - 2026-09-23

常驻系统托盘的网络配置方案（Profile）管理器，面向 macOS、Windows 与 Linux：
按身份而非网卡名称来匹配网络。

### 新增

- **配置方案模型。** 一个配置方案就是 `enabled` + `quick` + `detection` + `rules` + `THEN` + `ELSE`。规则之间
  是 OR；同一条规则内每个已启用的条件都必须满足（AND）。条件类型：`wifi_ssid`、
  `gateway_mac`、`bssid`、`network_interface`。每个条件、规则和动作都带有自己的 `enabled` 标志，
  并上报实时状态。
- **引擎。** 有且只有一个生效方案：命中 1 个就激活它，命中 2 个及以上是**冲突**（只报告，
  不应用任何一个），命中 0 个则应用全局 `fallback` 兜底。匹配失败（冲突）与执行失败（出错）是两种
  独立状态；重新求值由身份指纹驱动，该指纹同样能察觉网关与网卡集合的变化。
- **3A 网络配置作为一道整体屏障。** IPv4 / 子网掩码 / 网关 / DNS / IPv6 与静态路由一并下发，
  随后校验：回读系统并比对，可选带上 ICMP/HTTP 健康探测，持续失败时回落 DHCP。3A 失败会阻断 3B。
- **3B1 一次性动作**（`launch_app`、`run_script`、`set_default_printer`）按 `priority` 分批执行 —— 优先级数值小的先跑，
  同批内并发，批次之间顺序执行，某一批失败不会阻塞下一批。它们跑在引擎线程之外、各自带超时，
  每一轮执行都会留下一条结构化的逐动作结果，编辑器与面板据此显示实时状态。脚本路径受脚本白名单
  限制，相对路径在放行之前先按配置文件所在目录解析；提权脚本一律走系统授权对话框，一个动作一次授权。
  设默认打印机按**名字**指定（编辑器列出系统里的打印机，并标出当前默认的那台），改的也只是
  *当前用户*的默认值 —— macOS / Linux 用 `lpoptions`，Windows 用每用户的 CIM 方法 —— 所以切网络
  不会弹授权框。
- **3B2 常驻动作**（`periodic_script`、`keep_wireguard_connected`、`keep_vpn_connected`）维持的是一个期望
  状态，而不是把同一条命令重复执行：每条启用的动作一个 worker，随 Active 方案的 THEN 分支启动，并在任何
  新配置下发之前停掉。每次核对回答 `satisfied`、`repaired` 或 `faulted` —— 本来就绪的隧道一条命令都不会
  下发 —— 编辑器与面板按动作显示这个状态。worker 绝不触发授权弹窗：需要更高权限的隧道照实报错，
  而不是每个间隔问用户一次。
- **每个方案独立的检测方式**：`network_events`、`polling_only`，或两者都开，并各自带有变化后延迟，
  使一次瞬时重连不会反复折腾网卡。
- **托盘弹出面板**，含实时状态、每一张在用网卡、VPN 隧道、带命中标记的一键方案切换、设置、日志、
  DHCP/探测操作以及更新器。
- **两份配置文件，按「改了它会改变自动化行为吗」划分**：`config.json` 存自动化模型（Profile、条件、动作），
  由编辑器写入；`settings.json` 存应用本身怎么表现（界面语言、日志保留天数），在它自己的设置窗口里改。
  开机启动哪份文件都不存 —— 每次打开那个窗口都向操作系统问一次，因为在文件里留一份副本，就等于再造一个
  允许和系统各说各话的真相。
- **配置编辑器**覆盖整个模型，就地渲染引擎的判定结果，带有冲突对话框、三态 DNS 控件，以及手动应用前先列出 3A 目标与即将执行的那些批次。
- **平台抽象层**，在 macOS（`networksetup` / `ipconfig` / `arp` / `system_profiler`）、
  Windows（PowerShell CIM + `netsh`）与 Linux（`nmcli` / `ip`）上实现读取、写入、网卡枚举与提权；
  有免密通道时直接走，否则以系统授权对话框兜底。
- **在线升级**：检查新版本发布、原生安装并重启，进度实时流式推送到面板 —— 下载资源并校验 SHA256，
  或以 `brew upgrade --cask` 升级 Homebrew 安装。
- **五种界面语言**（en / zh / zh-TW / ja / ko），默认英语，并做键名对齐与占位符校验，在设置窗口里切换 ——
  每一座窗口、托盘提示与原生错误对话框都取自同一份字典；本地日志按天
  轮换，保留天数也在那个窗口里设；GitHub Actions 矩阵构建，一个标签即可产出全部四个目标的安装包。

### 已知缺口

- 隧道重连（`scutil` 的 `--nc` 子命令、`rasdial`、`wireguard.exe`、`nmcli`）目前只过了单元测试与各平台的
  CI 编译：与平台层的其余部分一样，它还需要每台真机各跑一遍；macOS 上某些 VPN 配置要先由用户手动连
  一次，之后才接受常驻动作发起的连接。
- 下发配置除了 DHCP 兜底之外没有回滚手段：3A 落地成功、随后健康探测持续失败的方案，
  会切回自动获取，而不是恢复上一份可用的配置。
