# 更新日誌

NetSense 的所有重要變更都記錄於此。格式依據
[Keep a Changelog](https://keepachangelog.com/)，本專案遵循
[語意化版本](https://semver.org/)。

## [未發佈]

### 變更

- **編輯器目前網路區塊**現在顯示完整的網卡詳情，與托盤面板一致（介面 → SSID → 訊號 → MAC → IPv4 → 子網路遮罩 → 閘道 → IPv6 → DNS）
- **SSID 選擇器**從 datalist 改為真正的下拉式選單，支援手動輸入隱藏網路
- **介面選擇器**顯示人類可讀的標籤（如 "en0 (Wi-Fi)"）而非裸設備名
- **印表機選擇器**從 datalist 改為真正的下拉式選單，顯示人類可讀標籤，排除自動發現的佇列
- 刪除未使用的 i18n key `editor.nic_gateway_mac`；新增 `editor.ssid_manual`、`editor.printer_placeholder`、`editor.printer_manual`

## [1.0.0] - 2026-09-23

常駐系統托盤的網路配置方案（Profile）管理員，面向 macOS、Windows 與 Linux：
以身分識別而非網卡名稱來匹配網路。

### 新增

- **配置方案模型。** 一個配置方案就是 `enabled` + `quick` + `detection` + `rules` + `THEN` + `ELSE`。規則之間
  是 OR；同一條規則內每個已啟用的條件都必須滿足（AND）。條件類型：`wifi_ssid`、
  `gateway_mac`、`bssid`、`network_interface`。每個條件、規則與動作都帶有各自的 `enabled` 旗標，
  並回報即時狀態。
- **引擎。** 有且只有一個生效方案：命中 1 個即啟用它，命中 2 個及以上為**衝突**（僅回報，
  不套用任何一個），命中 0 個則套用全域 `fallback` 後備。比對失敗（衝突）與執行失敗（出錯）是兩種
  獨立狀態；重新求值由身分指紋驅動，該指紋同樣能察覺閘道與網卡集合的變化。
- **3A 網路設定作為一道整體屏障。** IPv4 / 子網路遮罩 / 閘道 / DNS / IPv6 與靜態路由一併下發，
  接著校驗：回讀系統並比對，可選搭配 ICMP/HTTP 健康度探測，持續失敗時回落 DHCP。3A 失敗會阻擋 3B。
- **3B1 一次性動作**（`launch_app`、`run_script`、`set_default_printer`）依 `priority` 分批執行 —— 優先級數值較小的先跑，
  同批次內並行，批次之間循序執行，某一批失敗不會阻擋下一批。它們在引擎執行緒之外執行、各自帶有逾時，
  每一輪執行都會留下一筆結構化的逐動作結果，編輯器與面板據此顯示即時狀態。指令碼路徑受指令碼白名單限制，
  相對路徑在放行之前先依設定檔所在目錄解析；需提權的指令碼一律透過系統授權對話框，一個動作一次授權。
  設定預設印表機是按**名稱**指定（編輯器列出系統裡的印表機，並標出目前預設的那台），改的也只是
  *目前使用者*的預設值 —— macOS / Linux 用 `lpoptions`，Windows 用每位使用者的 CIM 方法 ——
  所以切換網路不會跳出授權框。
- **3B2 常駐動作**（`periodic_script`、`keep_wireguard_connected`、`keep_vpn_connected`）維持的是期望狀態，
  而不是把同一條命令重複執行：每個啟用的動作一個 worker，隨 Active 方案的 THEN 分支啟動，並在任何新設定
  下發之前停掉。每次核對回答 `satisfied`、`repaired` 或 `faulted` —— 本來就已就緒的通道一條命令都不會發出
  —— 編輯器與面板按動作顯示這個狀態。worker 絕不觸發授權彈窗：需要更高權限的通道照實回報錯誤，
  而不是每個間隔都問使用者一次。
- **每個方案獨立的偵測方式**：`network_events`、`polling_only`，或兩者並用，並各自帶有變化後延遲，
  使瞬時的重新連線不會反覆折騰網卡。
- **托盤彈出面板**，含即時狀態、每一張使用中的網卡、VPN 通道、帶命中標記的一鍵方案切換、設定、
  紀錄、DHCP/探測動作以及更新器。
- **兩份設定檔，依「改變它會不會改變自動化行為」劃分**：`config.json` 存自動化模型（方案、條件、動作），
  由編輯器寫入；`settings.json` 存應用本身怎麼表現（介面語言、日誌保留天數），在它自己的設定視窗裡改。
  登入時啟動兩份都不存 —— 每次打開那個視窗都向作業系統問一次，因為在檔案裡留一份副本，就等于再造一個
  允許和系統各說各話的真相。
- **配置編輯器**涵蓋整個模型，就地呈現引擎的判定結果，附帶衝突對話框、三態 DNS 控件，以及手動套用前先列出 3A 目標與即將執行的那些批次。
- **平台抽象層**，在 macOS（`networksetup` / `ipconfig` / `arp` / `system_profiler`）、
  Windows（PowerShell CIM + `netsh`）與 Linux（`nmcli` / `ip`）上實作讀取、寫入、網卡枚舉與提權；
  有免密碼通道時直接採用，否則以系統授權對話框作為後備。
- **線上升級**：檢查新版本發布、原生安裝與重新啟動，進度以串流方式推送到面板 —— 下載資源並驗證
  SHA256，或以 `brew upgrade --cask` 升級 Homebrew 安裝。
- **五種介面語言**（en / zh / zh-TW / ja / ko），預設為英文，並進行鍵名對齊與佔位符驗證，在設定視窗裡切換 ——
  每一座視窗、托盤提示與原生錯誤對話框都取自同一份字典；本機日誌依日
  輪替，保留天數也在那個視窗裡設定；GitHub Actions 矩陣建置，單一標籤即可產出全部四個目標的安裝包。

### 已知缺口

- 通道重連（`scutil` 的 `--nc` 子命令、`rasdial`、`wireguard.exe`、`nmcli`）目前只通過單元測試與各平台的
  CI 編譯：與平台抽象層的其餘部分一樣，它仍需每台真機各跑一次；macOS 上某些 VPN 設定要先由使用者手動
  連線一次，之後才接受常駐動作發起的連線。
- 下發配置除了 DHCP 後備之外沒有回滾手段：3A 落地成功、隨後健康度探測持續失敗的方案，
  會切回自動取得，而不是還原上一份可用的設定。
